# Server environment variables
# Copy this to .env and fill in real values

PORT=4000

# Origin allowed for CORS (leave blank in dev — localhost:5173 is whitelisted by default)
CLIENT_ORIGIN=

# OpenRouter API key (needed in Step 2 — AI module)
OPENROUTER_API_KEY=
